require "spec_helper"

describe Capybara::Guides do
  it "has a version number" do
    expect(Capybara::Guides::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
